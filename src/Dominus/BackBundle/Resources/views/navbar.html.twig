{% set navigation = {
    'Dashboard': {'href' : path('index')},
    'Utilisateurs': {'href': path('user_list')}
    'Données de jeu': {
        {#
          'Archétype héros': { 'href': path('hero_class_list') },
          'Guilde': {
                'Aménagements': {'href': path('guild_room_list')},
                'Bonus': {'href': 'path('guild_bonus_list')}
        #}
    }
}
} %}

{% set navigationRight = {
    (app.user.username) : {
        'submenu' : {
            'Modifier le mot de passe' : {'href' : path('fos_user_resetting_request')},
            'Déconnexion' : {'href' : path('logout')},
        }
    }
}
%}

{{ menu.navbar_left(navigation) }}
{{ menu.navbar_right(navigationRight) }}